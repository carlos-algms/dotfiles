;; extends
(
 (selectors) @selector (#set! "priority" 110)
 )

(class_name) @selector.class
(id_name)  @selector.id
(tag_name)  @selector.tag

(declaration
  (plain_value) @string.plain
  )

(unit) @unit


(arguments
  (plain_value) @string.plain
  )

(declaration
  (color_value) @color.value
  (#set! "priority" 111)
  )

