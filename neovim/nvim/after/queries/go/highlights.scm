;; extends


; it wasn't showing enum members as Constants
(selector_expression
  ; reduced priority to avoid override function calls
  field: (field_identifier) @variable.member (#set! "priority" 90)
  )
